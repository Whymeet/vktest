# 🛑 Управление процессами в Admin Panel

## Проблема

Раньше при остановке админ панели (Ctrl+C) дочерние процессы (scheduler и analysis) продолжали работать в фоне.

## ✅ Решение

Добавлены механизмы автоматической остановки всех дочерних процессов:

### 1. **Функция cleanup_processes()**
Автоматически останавливает все запущенные процессы при завершении программы:
- Использует `taskkill /F /T` на Windows для форсированного завершения дерева процессов
- На Linux/Mac использует `SIGTERM` → `SIGKILL` последовательность

### 2. **Регистрация через atexit**
```python
atexit.register(cleanup_processes)
```
Гарантирует выполнение cleanup при любом способе завершения Python.

### 3. **Обработчики сигналов**
Перехватывают сигналы завершения:
- `SIGINT` - Ctrl+C
- `SIGTERM` - системное завершение
- `SIGBREAK` - Ctrl+Break (Windows)

### 4. **Улучшенные функции остановки**
- `stop_scheduler()` - форсированная остановка планировщика
- `stop_analysis()` - форсированная остановка анализа (новая функция!)

### 5. **Кнопка остановки анализа**
Добавлена кнопка "⏹️ Остановить анализ" в веб-интерфейсе.

## 🎯 Как это работает

### При закрытии админ панели (Ctrl+C):
1. Перехватывается сигнал `SIGINT`
2. Вызывается `signal_handler()`
3. Выполняется `cleanup_processes()`
4. Все дочерние процессы принудительно завершаются
5. Программа корректно завершается

### При нажатии кнопки "Остановить":
1. Определяется PID процесса
2. На Windows: `taskkill /F /T /PID <pid>` - завершает весь процесс-tree
3. На Linux/Mac: `terminate()` → wait(5 сек) → `kill()` если не завершился

## 🔧 Технические детали

### Windows (taskkill)
```bash
taskkill /F /T /PID <process_id>
```
- `/F` - форсированное завершение
- `/T` - завершить дерево процессов (child processes)
- `/PID` - ID процесса

### Linux/Mac (сигналы)
```python
process.terminate()  # SIGTERM
process.wait(timeout=5)
# Если не завершился:
process.kill()  # SIGKILL
```

## ✅ Преимущества

1. **Никаких зависших процессов** - все дочерние процессы гарантированно завершаются
2. **Автоматическая очистка** - не нужно вручную останавливать процессы
3. **Кроссплатформенность** - работает на Windows, Linux, Mac
4. **Форсированное завершение** - даже "застрявшие" процессы будут остановлены
5. **Безопасность** - корректная обработка всех сигналов завершения

## 🧪 Тестирование

### Тест 1: Остановка через Ctrl+C
```bash
python admin_panel.py
# Запустите scheduler через веб-интерфейс
# Нажмите Ctrl+C в терминале
# Убедитесь, что scheduler тоже остановился
```

### Тест 2: Остановка через кнопку
```bash
python admin_panel.py
# Откройте http://127.0.0.1:5000/control
# Запустите планировщик → "▶️ Запустить планировщик"
# Остановите планировщик → "⏹️ Остановить планировщик"
# Проверьте что процесс завершился
```

### Тест 3: Проверка зависших процессов
**Windows PowerShell:**
```powershell
# Показать Python процессы
Get-Process python | Select-Object Id, ProcessName, StartTime

# После остановки админ панели:
Get-Process python | Select-Object Id, ProcessName, StartTime
# Должны остаться только те, что были до запуска
```

**Linux/Mac:**
```bash
# Показать Python процессы
ps aux | grep python

# После остановки админ панели:
ps aux | grep python
# Не должно быть процессов scheduler или main.py
```

## ⚠️ Важные замечания

### 1. Форсированное завершение
`taskkill /F` и `kill -9` **принудительно** завершают процесс без graceful shutdown.
Это необходимо, но может привести к:
- Незавершенным записям в лог
- Незакрытым файловым дескрипторам
- Незавершенным сетевым соединениям

**Решение**: В будущем можно добавить graceful shutdown с таймаутом.

### 2. DEBUG режим Flask
Flask в режиме `debug=True` использует reloader, который создает дополнительный процесс.
Текущее решение корректно обрабатывает это.

### 3. Subprocess flags
На Windows используется `CREATE_NEW_PROCESS_GROUP` для изоляции процессов.
Это позволяет `taskkill /T` корректно завершать дерево процессов.

## 🔮 Будущие улучшения

1. **Graceful shutdown** - дать процессам время на корректное завершение
2. **Логирование остановки** - записывать в лог причину остановки
3. **Status check** - проверять статус процессов перед завершением
4. **Timeout настройки** - возможность настроить таймауты ожидания
5. **Webhook уведомления** - отправлять уведомления в Telegram при аварийной остановке

## 📝 Связанные файлы

- `admin_panel.py` - основной файл с логикой управления процессами
- `templates/control.html` - веб-интерфейс управления
- `scheduler/scheduler_main.py` - скрипт планировщика
- `main.py` - скрипт анализа

## 🆘 Решение проблем

### Процессы все еще работают после остановки
```bash
# Windows: принудительно завершить все Python процессы
taskkill /F /IM python.exe

# Linux/Mac: найти и завершить процессы
pkill -9 python
```

### Ошибка "Access Denied" при остановке
- Запустите админ панель с правами администратора
- Или вручную завершите процесс через Task Manager

### Процесс "зависает" при остановке
- Увеличьте timeout в `cleanup_processes()` с 5 до 10 секунд
- Проверьте логи на наличие бесконечных циклов
