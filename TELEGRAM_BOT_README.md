# 🤖 Telegram Bot для управления VK Ads

## 📋 Описание

Telegram бот для управления рекламными кабинетами VK Ads через команды.

## 🚀 Установка

### 1. Установите необходимую библиотеку:

```bash
pip install python-telegram-bot
```

### 2. Убедитесь, что в `cfg/config.json` настроен Telegram:

```json
{
  "telegram": {
    "bot_token": "ВАШ_ТОКЕН_БОТА",
    "chat_id": "ВАШ_CHAT_ID",
    "enabled": true
  }
}
```

### 3. Запустите бота:

```bash
python telegram_bot.py
```

## 📱 Доступные команды

### `/start`
Приветственное сообщение и список доступных команд

### `/accounts`
Показывает список всех настроенных VK Ads кабинетов

### `/stop_cab [Название_Кабинета]`
Отключает **ВСЕ** активные рекламные группы в указанном кабинете

**Примеры:**
```
/stop_cab Кокос 1
/stop_cab Кроко-1 ZK
/stop_cab Кокос 2
```

## ⚙️ Режимы работы

### 🔸 Тестовый режим (DRY RUN)
Если в `config.json` установлено `"dry_run": true`, то команда `/stop_cab` только **покажет**, сколько групп было бы отключено, но **НЕ отключит** их реально.

### 🔴 Реальное отключение
Если в `config.json` установлено `"dry_run": false`, то команда `/stop_cab` **реально отключит** все активные группы в кабинете.

## 📊 Что происходит при выполнении `/stop_cab`

1. Бот загружает все активные группы из указанного кабинета через VK Ads API
2. Для каждой группы отправляется запрос на изменение статуса с `active` на `blocked`
3. Бот показывает прогресс и итоговую статистику:
   - Сколько групп отключено
   - Сколько ошибок
   - Общее количество обработанных групп

## 🔐 Безопасность

- Бот использует те же API токены, что и основной скрипт анализа
- Работает только с теми кабинетами, которые настроены в `config.json`
- Команды доступны только для chat_id, указанного в конфигурации (ваш личный чат)

## 🛠️ Настройка бота через BotFather

Если вы еще не создали бота:

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и сохраните полученный токен
4. Вставьте токен в `cfg/config.json` → `telegram.bot_token`

Чтобы узнать свой chat_id:

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Вставьте в `cfg/config.json` → `telegram.chat_id`

## 📝 Пример использования

```bash
# Запускаем бота
python telegram_bot.py

# В Telegram пишем:
/start              # Смотрим доступные команды
/accounts           # Смотрим список кабинетов
/stop_cab Кокос 1   # Отключаем все группы в кабинете "Кокос 1"
```

## 🔄 Запуск в фоновом режиме (опционально)

### Windows (PowerShell):
```powershell
Start-Process python -ArgumentList "telegram_bot.py" -WindowStyle Hidden
```

### Linux/Mac:
```bash
nohup python telegram_bot.py > telegram_bot.log 2>&1 &
```

## ⚠️ Важные замечания

1. **Бот работает постоянно** - он должен быть запущен, чтобы обрабатывать команды
2. **Одна команда = один кабинет** - нельзя отключить группы сразу во всех кабинетах одной командой
3. **Проверяйте dry_run** - перед реальным отключением убедитесь, что понимаете последствия
4. **Логи** - все действия бота записываются в `logs/vk_ads_manager_*.log`

## 🆘 Решение проблем

### Ошибка: "Не удается разрешить импорт telegram"
Установите библиотеку:
```bash
pip install python-telegram-bot
```

### Бот не отвечает
- Проверьте, что бот запущен (`python telegram_bot.py`)
- Проверьте правильность bot_token в config.json
- Убедитесь, что пишете с того chat_id, который указан в config.json

### Ошибка "Кабинет не найден"
- Используйте команду `/accounts` для просмотра точных названий
- Название должно совпадать **точно** (включая регистр и пробелы)
- Пример правильно: `/stop_cab Кокос 1` (с пробелом)
- Пример неправильно: `/stop_cab Кокос1` (без пробела)

## 🔮 Будущие возможности

Потенциальные команды для добавления:
- `/start_cab [Name]` - включить все группы
- `/status_cab [Name]` - статистика по кабинету
- `/list_groups [Name]` - список групп в кабинете
- `/stop_group [ID]` - отключить конкретную группу
