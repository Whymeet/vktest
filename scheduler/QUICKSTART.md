# 🚀 Быстрый старт VK Ads Scheduler

## Что это?

Автоматический планировщик, который будет запускать анализ VK Ads каждые N минут без вашего участия.

## ⚡ Быстрая настройка

### 1. Включить планировщик

Отредактировать `cfg/config.json`:
```json
{
  "scheduler": {
    "enabled": true,
    "interval_minutes": 60
  }
}
```

### 2. Запустить

**Windows:**
```cmd
python scheduler/scheduler_main.py
```

**Или как сервис:**
```cmd
scheduler\scheduler_service.bat start
```

### 3. Проверить статус

```cmd
python scheduler/scheduler_main.py --status
```

## 📋 Основные команды

| Команда | Описание |
|---------|----------|
| `python scheduler/scheduler_main.py` | Запуск планировщика |
| `python scheduler/scheduler_main.py --status` | Статус планировщика |
| `scheduler\scheduler_service.bat start` | Запуск как сервис |
| `scheduler\scheduler_service.bat stop` | Остановка сервиса |
| `scheduler\scheduler_service.bat status` | Статус сервиса |

## ⚙️ Полезные настройки

### Каждые 30 минут с тихими часами
```json
{
  "scheduler": {
    "enabled": true,
    "interval_minutes": 30,
    "quiet_hours": {
      "enabled": true,
      "start": "23:00",
      "end": "08:00"
    }
  }
}
```

### Тестовый режим (3 раза по 5 минут)
```json
{
  "scheduler": {
    "enabled": true,
    "interval_minutes": 5,
    "max_runs": 3
  }
}
```

## 📝 Логи

- **Планировщик:** `scheduler/logs/vk_ads_scheduler_*.log`
- **Основной анализ:** `logs/vk_ads_manager_*.log`

## 🛑 Остановка

- **Ctrl+C** в консоли
- `scheduler\scheduler_service.bat stop`
- Установить `"enabled": false` в config.json

## ⚠️ Важно

1. **Планировщик отключен по умолчанию** - нужно явно включить
2. **Использует тот же конфиг** что и основная система  
3. **Запускает полный анализ** включая Telegram уведомления
4. **Автоматически повторяет** при ошибках (по умолчанию)